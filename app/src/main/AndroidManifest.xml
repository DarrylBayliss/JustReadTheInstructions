<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.justreadtheinstructions">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".JustReadTheInstructionsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JustReadTheInstructions">
        <activity
            android:name=".presentation.detail.LaunchesDetailActivity"
            android:exported="true"
            android:label="@string/title_activity_launches_detail"
            android:theme="@style/Theme.JustReadTheInstructions.NoActionBar" />
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JustReadTheInstructions.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>